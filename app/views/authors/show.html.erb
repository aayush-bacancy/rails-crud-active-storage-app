<p style="color: green"><%= notice %></p>
<div class="p-2">
  <%= render @author %>
</div>
<div class="d-flex justify-content-between algin-items-center p-2">
  <%= link_to "Edit this author", edit_author_path(@author), class: "btn btn-warning" %>
  <%= button_to "Destroy this author", @author, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
</div>
