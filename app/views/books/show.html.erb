<p style="color: green"><%= notice %></p>
<div class="p-2">
  <%= render @book %>
</div>
<div class="d-flex justify-content-between algin-items-center p-2">
  <%= link_to "Edit this book", edit_book_path(@book), class: "btn btn-warning" %>
  <%= button_to "Destroy this book", @book, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
</div>
